# MySQL Database Configuration for SorrentoMarina
# These environment variables are used by docker-compose.yml

# Database Connection Settings
DB_HOST=mysql-db
DB_PORT=3306
DB_NAME=sorrentomarina
DB_USER=sorrentouser
DB_PASSWORD=sorrentopass

# MySQL Root Password (for administrative access)
MYSQL_ROOT_PASSWORD=rootpassword

# Application Settings
CATALINA_OPTS=-Xmx1024m -Xms512m -server
JAVA_OPTS=-Djava.awt.headless=true -Djava.security.egd=file:/dev/./urandom